name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  id-token: write

env:
  NODE_VERSION: '24.13.0'
  NPM_CONFIG_PROVENANCE: true

jobs:
  release:
    name: Release to npm
    runs-on: ubuntu-latest
    environment: production
    
    steps:
      - name: Mint GitHub App token
        id: app-token
        # Only mint when org-level RELEASE_APP_ID is set. `continue-on-error`
        # makes the workflow degrade-safe: if the App isn't installed on this
        # repo yet, or secrets are missing, mint fails non-fatally and the
        # fallback chain picks up RELEASE_TOKEN.
        if: ${{ vars.RELEASE_APP_ID != '' }}
        continue-on-error: true
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          # PAT with `workflow` scope when available — GITHUB_TOKEN cannot push
          # tags on commits touching .github/workflows/* (server-side reject).
          # Falls back cleanly; degraded mode no-ops tag push for such releases.
          token: ${{ steps.app-token.outputs.token || secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: 'https://registry.npmjs.org'
          cache: ''  # No cache - package is part of workspace
      
      - name: Install dependencies
        run: |
          npm install
          npm install -D @changesets/cli @changesets/changelog-github
      
      - name: Upgrade npm for Trusted Publishing
        # Bounded to 11.x on purpose: npm 12 requires node
        # `^22.22.2 || ^24.15.0 || >=26.0.0`, which the 24.13.0 pin above does not
        # satisfy — so `npm@latest` aborts with EBADENGINE and fails the release
        # outright. `^11` keeps the >=11.5 OIDC floor this step exists for.
        # Widen once the node pin moves to >=24.15.0.
        run: npm install -g npm@^11
      
      - name: Verify npm version and OIDC support
        run: |
          NPM_VERSION=$(npm --version)
          echo "npm version: $NPM_VERSION"
          
          # Extract major and minor version numbers
          NPM_MAJOR=$(echo $NPM_VERSION | cut -d. -f1)
          NPM_MINOR=$(echo $NPM_VERSION | cut -d. -f2)
          
          # Check if version >= 11.5
          if [ "$NPM_MAJOR" -lt 11 ] || { [ "$NPM_MAJOR" -eq 11 ] && [ "$NPM_MINOR" -lt 5 ]; }; then
            echo "❌ npm version must be >= 11.5 for OIDC support (found: $NPM_VERSION)"
            exit 1
          fi
          echo "✅ npm version $NPM_VERSION compatible (>= 11.5)"
          
          echo "Checking OIDC environment:"
          if env | grep -q ACTIONS_ID_TOKEN; then
            echo "✅ OIDC token available"
          else
            echo "⚠️ OIDC token not found (may not be needed at this step)"
          fi
      
      - name: Run linter
        run: npm run lint
        continue-on-error: true
      
      - name: Type check
        run: npm run typecheck
      
      - name: Run tests
        run: npm run test:run
      
      - name: Build package
        run: npm run build
      
      - name: Create Release Pull Request or Publish
        uses: changesets/action@v2
        with:
          # v2 ignores the GITHUB_TOKEN env var — the token must be an input. Same
          # App → PAT → GITHUB_TOKEN chain as checkout, so release commits, tags and
          # the Version Packages PR are attributed to the release App.
          github-token: ${{ steps.app-token.outputs.token || secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}
          publish-script: npm run release
